# Sixth City Marketing — AI Toolkit

Internal AI tool library. Static page that reads tool data live from a published Google Sheet.

- `index.html` — the page
- `assets/` — logo + icon

Edit tools in the shared Google Sheet; the page updates on refresh.
